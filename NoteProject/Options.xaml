<Window x:Class="NoteProject.Options"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NoteProject"
        mc:Ignorable="d"
        Title="Options" Height="100" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>

            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Podaj nazwę biblioteki docelowej:" Grid.Column="0" Grid.Row="0" Background="{StaticResource Description}" FontSize="14"/>
        <TextBlock Background="{StaticResource Description}" Grid.Row="1" Grid.Column="0"/>
        <TextBox x:Name="filename" Grid.Column="1" Grid.Row="0" Background="{StaticResource TCback}"/>
        <Button Content="Zatwierdź" Click="Button_Click" Grid.Column="1" Grid.Row="1" FontSize="14"/>
    </Grid>
</Window>
